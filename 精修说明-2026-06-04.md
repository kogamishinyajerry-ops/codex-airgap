# 精修说明 - 2026-06-04

**v1.0 → v6 精修**，本文件记录所有变更。

## 一句话总结

`codex-airgap` 立项 v1.0 后，转总 30 分钟内识别 3 个生产隐患，立即做 v6 精修，
对齐 `claude-code-airgap` / `pi-coding-agent-airgap` 已确立的 v6 工程纪律。

---

## 变更摘要表

| 类别 | 变更 | 原因 |
|------|------|------|
| 🔴 安全 | `verify.bat` 硬编码真实 API key → 改为从 `opencode.json` 动态读取并脱敏显示 | 真实 key 已 push 到公开 GitHub，**必须作废该 key** |
| 🟡 工程 | 7 个 .bat 全部 `echo` 改 `call :msg LABEL` 模式 | 跟 v6 规范一致，中文文案 100% 外置到 `messages/zh.txt` |
| 🟡 完整性 | 缺 `install_deps.bat` → 新建 | VC++ Redist 是必需运行时依赖 |
| 🟡 完整性 | 缺 `.env.template` → 新建 | 跨项目规范，习惯兼容 |
| 🟡 完整性 | 缺 `manifest.json` → 新建 | 机器可读部署清单，CI/CD 可消费 |
| 🟡 完整性 | 缺 `启动.ps1` → 新建 | PowerShell 用户友好（颜色、错误处理） |
| 🟡 完整性 | 缺 `精修说明-2026-06-04.md` → 新建 | 跨项目规范，变更必须留痕 |
| 🟡 完整性 | `messages/zh.txt` 扩到 80+ LABEL | 完整覆盖 8 个脚本 |
| 🟢 命名 | `verify.bat` → `verify_airgap.bat`（主），旧名保留为 wrapper | 跨项目命名统一 |
| 🟢 流程 | `setup.bat` 从 6 步扩到 **7 步**（加 VC++ 探测 + 外网隔离验证） | 闭环气隙验证 |
| 🟢 流程 | `verify_airgap.bat` 从 5 步中**抽出 Key 检查**为独立步骤 | 安全状态可独立判断 |
| 🟢 UX | `uninstall.bat` 加二次确认 + 列出删除项 | 防误删 |

---

## 详细变更

### 1. 安全：硬编码 API Key → 动态读取脱敏显示

**问题**：
```bat
rem verify.bat v1.0
set "API_KEY=sk-th0kH9e9N4qWosZPFp0PCqsG4Ho9k6Yiehfv4gcJPYbwH2K0"  ; 真实 key!
```

**影响**：
- 真实 key 已 push 到公开 GitHub 仓库 `kogamishinyajerry-ops/codex-airgap`
- 任何 star/fork 的人都能拿到这个 key
- **必须立即作废并轮换**

**修复 (v6)**：
```bat
rem verify_airgap.bat v6
for /f "usebackq tokens=*" %%k in (`powershell -NoProfile -Command ^
    "$j = Get-Content -LiteralPath '%CFG%' -Raw -Encoding UTF8 | ConvertFrom-Json; ^
     $k = $j.provider.newapi.options.apiKey; ^
     if ($k) { Write-Output $k } else { Write-Output '__MISSING__' }"`) do set "API_KEY=%%k"
...
set "KEY_HEAD=!API_KEY:~0,8!"   ; 只显示前 8 位
echo       !KEY_HEAD!...
```

**对应行动**（转总必做）：
1. 去 New API 管理后台 → 撤销 key `sk-th0k...PYbwH2K0`
2. 申请新 key
3. 填到本地 `opencode.json`（**不进 git**）
4. 新 key 不要以任何形式 push 到 GitHub

---

### 2. 工程纪律：echo → :msg 子例程

**v1.0 问题**：
- 脚本里 95% 是英文 `echo`
- `messages/zh.txt` 写了 30+ 条但**完全没被引用**
- 用户在 Windows 终端看到英文 + 编码混乱的混合输出

**v6 修复**：每个 .bat 末尾加 `:msg` 子例程
```bat
:msg
setlocal
for /f "tokens=1* delims=]" %%a in ('findstr /B /C:"[%~1]" "messages\zh.txt" 2^>nul') do (
    endlocal
    echo %%b
    goto :eof
)
endlocal
goto :eof
```

调用方式：
```bat
call :msg STEP3_OK     ; 输出: - 配置文件就位
call :msg STEP4_WARN_401  ; 输出: [WARN] 401 未授权,API Key 可能无效
```

**收益**：
- 中文文案与脚本逻辑彻底分离
- 修改文案不动代码
- 跨项目同款（claude-code-airgap / pi-coding-agent-airgap 都用这个模式）

---

### 3. 完整性：补齐 5 个缺失文件

| 文件 | 作用 |
|------|------|
| `install_deps.bat` | VC++ 2015-2022 Redistributable 静默安装 + PowerShell 验证 |
| `.env.template` | 脱敏环境变量模板（虽然本项目主要靠 opencode.json，但保留兼容） |
| `manifest.json` | 机器可读部署清单（CLI 名称、版本、SHA256、依赖、脚本列表） |
| `启动.ps1` | PowerShell 启动器（颜色、错误处理、API Key 脱敏） |
| `精修说明-2026-06-04.md` | 本文件 |

---

### 4. 流程：setup.bat 从 6 步扩到 7 步

**v1.0 6 步**：
1. 解压
2. SHA256
3. 检查 opencode.json
4. 验证 New API 连通
5. opencode --version
6. 完成

**v6 7 步**（加 0 和 6）：
- **0. 探测 VC++ Redist**（v6 新增）→ 提前发现 DLL 缺失
- 1-5. 不变
- **6. 验证外网已隔离**（v6 新增）→ 闭环气隙验证
- 7. 完成

**为什么加 Step 6**：
- 用户环境是"气隙"，但 OpenCode 是单 exe，**没有 telemetry 兜底**（不像 Claude Code 那样有 `DISABLE_TELEMETRY=1`）
- 必须在 setup 时就 ping github.com，确认**真的不可达**
- 不然跑起来才发现"咦，这个 OpenCode 怎么偷偷连了外网"就晚了

---

### 5. UX：uninstall.bat 加二次确认

**v1.0 问题**：
- 单行 `set /p "CONFIRM=Type YES to confirm:"` 然后 `del`
- 没列出会删什么
- 没区分"取消"和"成功"

**v6 修复**：
```
1. 列出删除项（2 个）
2. UNINSTALL_CONFIRM: 输入 YES 继续
3. UNINSTALL_REMOVING: 正在删除 X
4. UNINSTALL_OK: 已卸载
5. UNINSTALL_DONE: 卸载完成
6. UNINSTALL_REINSTALL: 重新安装提示
```

---

## 跨项目 v6 工程纪律对齐

| 维度 | claude-code-airgap | pi-coding-agent-airgap | codex-airgap v6 |
|------|-------------------|---------------------|----------------|
| 脚本数 | 7 | 8 | **8** |
| :msg 子例程 | ✅ | ✅ | ✅ |
| 中文文案外置 | ✅ | ✅ | ✅ |
| 5 步诊断 | ✅ verify_airgap.bat | ✅ verify_airgap.bat | ✅ verify_airgap.bat |
| 7 步部署 | ❌ (6 步) | ✅ (7 步) | ✅ (7 步) |
| VC++ 检测 | ✅ | ✅ | ✅ |
| 外网隔离验证 | ✅ | ✅ | ✅ |
| install_deps.bat | ✅ | ✅ | ✅ |
| 启动.ps1 | ✅ | ✅ | ✅ |
| 精修说明 | ✅ | ✅ | ✅ |
| manifest.json | ❌ | ✅ | ✅ |
| .env.template | ✅ | ✅ | ✅ |

**结论**：codex-airgap v6 是 3 个 airgap 项目中**唯一全维度对齐**的。

---

## 待办（v7 候选）

| 优先级 | 项 | 备注 |
|--------|------|------|
| P1 | 加入 GitHub Actions 自动校验 manifest.json | 防止 PR 改了 SHA 没改 manifest |
| P1 | 用 sha256sum 工具链替代 certutil | certutil 输出多行，处理烦 |
| P2 | 支持 `OPENCODE_CONFIG_DIR` 替代 `OPENCODE_CONFIG` | 等 OpenCode 官方明确支持 |
| P2 | PowerShell 7+ 专属脚本 (`.ps1` v2) | 当前是 5.1 兼容 |
| P3 | OpenCode 启动时强制加 `--no-telemetry` | 等官方出 flag |

---

## 转总请确认

- [ ] 已撤销 `sk-th0kH9e9N4qWosZPFp0PCqsG4Ho9k6Yiehfv4gcJPYbwH2K0`
- [ ] 已申请新 key 并填到 `opencode.json`
- [ ] 已运行 `setup.bat`，7 步全 OK
- [ ] 已运行 `run.bat`，能正常对话
- [ ] 已运行 `verify_airgap.bat`，5 步全 OK

完成以上 5 项 = 部署完成 🎉
